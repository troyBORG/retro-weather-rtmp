# Copy to `.env` and set your ZIP. `.env` is gitignored.
LOCATION=60601

# Optional: IANA timezone (e.g. America/Chicago). If unset, the container picks TZ from the US ZIP above.
# TZ=America/Chicago
